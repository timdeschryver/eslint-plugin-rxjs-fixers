import operatorWith from './operator-with'
import resultSelectors from './result-selectors'
import subscribeSignature from './subscribe-signature'
import tapSignature from './tap-signature'

export default {
  'operator-with': operatorWith,
  'result-selectors': resultSelectors,
  'subscribe-signature': subscribeSignature,
  'tap-signature': tapSignature,
}
